galaxy_info:
  role_name: shell
  namespace: initialgyw
  author: konri
  description: portable shell config for myself
  company: GYWadmin
  license: MIT

  min_ansible_version: "2.0"

  galaxy_tags:
    - shell
    - zsh
    - bash
    - fzf

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
